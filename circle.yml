machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t comet . 

test:
  override:
    - docker run -t -v `pwd`:/notebook comet bash -c "/get_html.sh":
        timeout: 900
    - mv COMET.html $CIRCLE_ARTIFACTS
