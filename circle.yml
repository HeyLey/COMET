machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t comet . 

test:
  override:
    - docker run -t comet -v `pwd`:/notebook bash -c "/get_html.sh":
        timeout: 900
    - mv COMET.html $CIRCLE_ARTIFACTS
